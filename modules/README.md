# modules

本目录保存 CloudHelm 后端与平台能力模块。M1 只实现 `platform-api` 的最小 FastAPI 工程；后续会按设计文档继续加入 orchestrator、agent-runtime、tool-gateway、sandbox-runner、remote-agent、deployment-controller、monitoring-collector 等模块。
